# Variables de entorno para Inventory POS (copiar a backend/.env.local o backend/.env)
# Nunca publiques credenciales reales en el repositorio.

# Cadena de conexión MongoDB (Atlas o local)
MONGODB_URI=mongodb+srv://<usuario>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# Puerto donde corre el backend
PORT=4000

# Clave para firmar JWT (usa un valor largo y único en producción)
JWT_SECRET=define_un_secreto_seguro
